{
  "name": "mcs-visual",
  "displayName": "Mcs-Visual",
  "description": "Visual-editor for dirMcsh Mcs*/Hitp*.last.html files. Renders the live local-server page, lets you edit the whole page, and on Save re-serializes the WHOLE document to the canonical 2/4-space Mcsh format.",
  "version": "0.3.0",
  "date": "2026-09-03",
  "publisher": "synagonism",
  "private": true,
  "license": "MIT",
  "icon": "media/icon128.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/synagonism/dirMcsmgr"
  },
  "engines": {
    "vscode": "^1.134.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:mcsv.currentFile",
    "onCommand:mcsv.currentFileDirname",
    "onCommand:mcsv.currentFileBasename"
  ],
  "main": "./extension.js",
  "contributes": {
    "customEditors": [
      {
        "viewType": "mcsv.editorVisual",
        "displayName": "Mcs-Visual",
        "selector": [
          {
            "filenamePattern": "Mcs*.last.html"
          },
          {
            "filenamePattern": "Hitp*.last.html"
          }
        ],
        "priority": "option"
      }
    ],
    "commands": [
      {
        "command": "mcsv.open",
        "title": "Open in Mcs-Visual",
        "category": "Mcs",
        "icon": "media/icon.png"
      },
      {
        "command": "mcsv.openByCode",
        "title": "Open McsHitp file by code",
        "category": "Mcs"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "mcsv.open",
          "when": "resourceExtname == .html && resourceFilename =~ /\\.last\\.html$/",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Mcs-Visual",
      "properties": {
        "mcsv.serverOrigin": {
          "type": "string",
          "default": "http://localhost",
          "description": "Origin of the local web server that renders the pages (e.g. http://localhost)."
        },
        "mcsv.docRootFolder": {
          "type": "string",
          "default": "htdocs",
          "description": "Folder name in the file path that marks the server document-root. The URL path is everything after it."
        },
        "mcsv.keybindingsPath": {
          "type": "string",
          "default": "",
          "description": "Path to your VS Code keybindings.json. The viewer auto-syncs its wrap/insert chord shortcuts from it. Leave empty to use the default per-OS user path."
        }
      }
    }
  },
  "dependencies": {
    "parse5": "^7.1.2"
  },
  "devDependencies": {
    "@types/vscode": "^1.134.0"
  }
}
